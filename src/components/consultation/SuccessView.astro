---
const { successMsg } = Astro.props;
---

<div class="min-h-screen flex items-center justify-center relative">
  <div
    class="w-full h-full absolute inset-0 bg-gradient-to-br from-green-50 via-green-100 to-green-50 flex items-center justify-center"
  >
    <div
      class="relative max-w-xl mx-auto px-6 py-10 bg-white border border-green-200 rounded-xl shadow-lg text-center z-10"
    >
      <svg
        class="mx-auto mb-4 w-14 h-14 text-green-600"
        fill="none"
        stroke="currentColor"
        stroke-width="1.5"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          d="M4.5 12.75l6 6 9-13.5"></path>
      </svg>

      <h1 class="text-3xl font-bold text-green-800">Success!</h1>
      <p class="mt-2 text-lg text-green-700">{successMsg}</p>
      <p class="mt-1 text-green-600">We’ll get back to you shortly.</p>

      <div class="mt-6">
        <a
          href="/"
          class="inline-block px-6 py-2 bg-green-600 text-white font-medium rounded-md hover:bg-green-700 transition"
        >
          Go to home page
        </a>
      </div>
    </div>
  </div>
</div>
